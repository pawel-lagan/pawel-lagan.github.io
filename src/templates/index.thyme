<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-strict-thymeleaf-4.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org" class="dark bg-zinc-800 text-zinc-400 kanit-regular">
<head th:replace="header.thyme::head" />

<body>
    <div th:replace="menu.thyme::menu" />

    <div th:replace="logo.thyme"></div>

    <div class="ol-container">
        <th:block th:with="total_posts = *{#lists.size(published_posts)}">
            <th:block th:if="*{total_posts != 0}" th:with="max_posts = 3, first_post = *{posts[0]}">

            <h2>Recent articles</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">

                <th:block th:each="i : *{#numbers.sequence( 0, max_posts)}">
                    <div class="py-4" th:with="post = *{posts[i]}">

                        <a th:href='${post.uri}' href='href'>
                            <h3 class="text-xl" th:text='${post.title}'>title</h3>
                            <p class="text-zinc-600 text-xs" th:text='${#dates.format(post.date,"dd MMMM yyyy")}'>date</p>
                        
                        
                            <img class="py-4" th:if='${post.short_img}' th:src="${post.short_img}"/>
                            <p th:utext='${post.short}'>short description</p>
                        </a>
                    </div>
                </th:block>
            </div>

            <p class="text-sm">Older posts are available in the <a th:href='${content.rootpath+config.archive_file}' href='href'>artices</a>.</p>

            </th:block>
        </th:block>
    </div>

    <div th:replace="footer.thyme::footer"></div>
</body>

</html>