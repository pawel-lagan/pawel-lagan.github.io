<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-strict-thymeleaf-4.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org" class="dark bg-zinc-800 text-zinc-400 kanit-regular">
<head th:replace="header.thyme::head" />

<body>
    <div th:replace="menu.thyme::menu" />
    <div class="ol-container">
        <h1>Projects</h1>           

        <div class="grid grid-cols-1 grid-flow-row gap-8">
            <th:block th:each="anypage: ${published_content}">
                <th:block th:if="*{anypage.type == 'project'}" th:with="project = *{anypage}">    
                    <div class="py-4">
                        <a th:href='${project.uri}' href='href'>
                        <h3 class="text-xl" th:text='${project.title}'>title</h3>                    
                        <p class="text-zinc-600 text-xs" th:text='${#dates.format(project.date,"dd MMMM yyyy")}'>date</p>
                        
                        <img class="py-4" th:if='${project.short_img}' th:src="${project.short_img}"/>
                        <p th:utext='${project.short}'>short description</p>
                        </a>                                        
                    </div>

                </th:block>
            </th:block>
            
        </div>
    </div>

    <div th:replace="footer.thyme::footer"></div>
</body>

</html>